#include <stdio.h>

int main() {
    int num, i;

    printf("Enter a number: ");
    scanf("%d", &num);

    printf("The next 5 successive numbers are:\n");
    for (i = 1; i <= 5; i++) {
        printf("%d ", num + i);
    }
    printf("\n");

    return 0;
}
